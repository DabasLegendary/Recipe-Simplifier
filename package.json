{
  "name": "recipe-matrix-backend",
  "version": "1.0.0",
  "description": "Backend for Recipe Matrix — keeps the Anthropic API key server-side and serves the frontend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
